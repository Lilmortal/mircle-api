spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  database:
    driverClassName: org.postgresql.Driver

---

spring:
  profiles: dev
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create
  database:
    driverClassName: org.postgresql.Driver
  datasource:
    platform: postgres
    url: jdbc:postgres://udiocpgdlcpnna:71962c1c0cbd89a0989e2c82b27a637267abe43aae803665e6db18cf53c0b30a@ec2-54-221-220-82.compute-1.amazonaws.com:5432/d389atkt3e18rk
    username: ${POSTGRESQL_USERNAME}
    password: ${POSTGRESQL_PASSWORD}
  server:
    port: 80
---

spring:
  profiles: test
  jpa:
    database: platform
    show-sql: true
    hibernate:
      ddl-auto: create
  database:
    driverClassName: org.postgresql.Driver
  datasource:
    platform: postgres
    url: jdbc:postgresql://aa1ke7ocav1pcw3.cbmwsuphlpqy.ap-southeast-2.rds.amazonaws.com:5432/aa1ke7ocav1pcw3
    username: ${POSTGRESQL_USERNAME}
    password: ${POSTGRESQL_PASSWORD}

---

spring:
  profiles: prod
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: validate
  database:
    driverClassName: org.postgresql.Driver
  datasource:
    platform: postgres
    url: jdbc:postgresql://mircle.cbmwsuphlpqy.ap-southeast-2.rds.amazonaws.com:5432/mircle
    username: ${POSTGRESQL_USERNAME}
    password: ${POSTGRESQL_PASSWORD}